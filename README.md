Run Application :
                cd backend
                uvicorn app.main:app --reload --port 8080


                -- install awscli
                -- build docker image
                -- build the same database in AWS RDS same password same name (MY SQL)
                -- build the docker image on EC2
                -- create docker network
                -- run then docker container and check logs as welll 
                --get aws IP and check it in browser